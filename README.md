# Paolopapichulo

Site vitrine — agence de croissance sur les réseaux sociaux.
Site statique (HTML + Tailwind CDN). Déployé via Cloudflare Pages.

- `index.html` — la page
- `favicon.svg` — le logo / favicon
